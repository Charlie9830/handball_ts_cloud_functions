class LinkingCode {
    constructor(
        public code: string,
        public projectName: string,
        public projectId: string,
        public inviteTimestamp: Date
        ) {
    }
}

export default LinkingCode;